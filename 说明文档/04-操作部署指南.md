# 操作部署指南

## 1. 环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| Flutter | 3.x | Dart SDK >= 3.0 |
| Python | 3.11+ | 推荐 3.12；3.11-slim 为 Docker 基线 |
| Node.js | 18+ | 仅旧后端需要，Python 服务不依赖 |
| SQLite | 内嵌 | Flutter 端 sqflite，Python 端 sqlite3 标准库 |

## 2. Python 导入后端

### 2.1 本地开发

```bash
cd backend/python_import_service

# 创建虚拟环境（首次）
python -m venv .venv

# 激活环境
# Windows:
.venv\Scripts\activate
# macOS/Linux:
# source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 启动开发服务器（带热重载）
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

启动后访问：
- 健康检查：`http://localhost:8000/health`
- API 文档：`http://localhost:8000/docs`（FastAPI 自动生成 Swagger UI）
- 学校列表：`http://localhost:8000/v1/config/schools`

### 2.2 运行测试

```bash
cd backend/python_import_service
pytest tests/ -v
```

当前覆盖 6 个测试用例：
1. `test_list_schools_v1` — 学校列表 v1 兼容格式
2. `test_get_school_config` — 学校配置详情
3. `test_get_provider_script` — JS 脚本下载
4. `test_validate_import_success` — 导入校验成功（2 门课，含范围周 + 双周）
5. `test_validate_import_invalid_json` — 导入校验失败（无效 JSON）
6. `test_report_log_v1` — v1 格式日志上报

### 2.3 Docker 部署

```bash
cd backend/python_import_service

# 构建镜像
docker build -t classduck-import-service .

# 运行容器
docker run -d -p 8000:8000 --name classduck-import classduck-import-service
```

> 注意：Dockerfile 当前基于 `python:3.11-slim`。若安全扫描报告基础镜像漏洞，
> 可切换为 `python:3.12-slim` 或 Alpine 变体。

### 2.4 新增学校

1. 在 `data/school_configs/` 下新建 `{school_id}.json`：
   - 参照 `xjtu.json` 格式填写 `field_mapping`、`fetch_config`、`timer_config` 等
   - `id` 字段必须与文件名（不含扩展名）一致
2. 在 `data/scripts/` 下新建 `{school_id}_provider.js`：
   - 脚本在 WebView 内执行，通过 `window.flutter_inappwebview.callHandler` 回传数据
   - 支持 `{{YEAR}}` / `{{TERM}}` 占位符
3. 重启服务：新增的配置和脚本会在启动时自动加载

### 2.5 关键文件说明

```
python_import_service/
├── main.py                  # 入口：CORS、路由注册、全局异常
├── api/
│   ├── schools.py           # /v1/config/schools、/api/schools/{id}/config|script
│   ├── import_handler.py    # POST /api/import/validate（核心校验）
│   ├── logs.py              # POST /v1/import/logs、POST /api/import/log
│   └── release.py           # GET /v1/release/check
├── services/
│   ├── school_service.py    # 文件驱动的配置加载 + 内存缓存
│   ├── validator_service.py # 字段映射 + 周次/节次解析 + 标准化
│   ├── log_service.py       # SQLite 日志持久化
│   └── release_service.py   # 版本号比对
├── models/                  # Pydantic 模型（SchoolConfig/StandardCourse/ImportLog）
├── schemas/                 # 请求体 Schema（ValidateRequest）
├── data/
│   ├── school_configs/      # 学校 JSON 配置（如 xjtu.json）
│   ├── scripts/             # JS 注入脚本（如 xjtu_provider.js）
│   └── db/                  # SQLite 日志数据库（自动创建）
└── tests/                   # pytest 测试
```

## 3. Flutter 客户端

### 3.1 本地开发

```bash
cd classduck_app

# 获取依赖
flutter pub get

# 静态分析
flutter analyze

# Web 运行
flutter run -d chrome

# Windows 运行
flutter run -d windows

# Android 运行（需连接设备或模拟器）
flutter run -d <device_id>
```

### 3.2 后端连接配置

文件：`lib/app/config/app_env.dart`

```dart
class AppEnv {
  static const String apiBaseUrl = 'http://localhost:8000';
}
```

不同环境的配置：
| 场景 | apiBaseUrl |
|------|------------|
| Windows/macOS 本机 | `http://localhost:8000` |
| Android 真机（同一 WiFi） | `http://<电脑IP>:8000` |
| Android 模拟器 | `http://10.0.2.2:8000` |
| iOS 模拟器 | `http://localhost:8000` |
| 生产环境 | `https://api.classduck.com` |

### 3.3 导入链路双通路说明

Flutter 导入执行页 (`import_execution_page.dart`) 支持两条导入通路：

**新通路（推荐）—— 后端校验**
1. WebView 打开教务登录页
2. 用户登录后，注入 JS 脚本获取原始课表 JSON
3. `_rawJsonFromJs` 存入原始数据
4. 点击导入 → `ImportEngine.importFromRawJson()` → POST `/api/import/validate` → 存入 SQLite

**旧通路（兜底）—— 本地解析**
1. WebView 打开教务登录页
2. 用户登录后，抓取 HTML 页面源码
3. `_capturedHtml` 存入 HTML
4. 点击导入 → `ImportEngine.importFromCapturedHtml()` → Dart 本地 parser → 存入 SQLite

选择逻辑：`_rawJsonFromJs` 有值则走新通路，否则走旧通路。

### 3.4 构建发布

```bash
# Web
flutter build web

# Android APK
flutter build apk --release

# Android App Bundle
flutter build appbundle --release

# Windows
flutter build windows --release

# iOS（需 macOS + Xcode）
flutter build ipa --release
```

## 4. 端到端联调步骤

1. **启动 Python 后端**
   ```bash
   cd backend/python_import_service
   .venv\Scripts\activate
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
   ```

2. **验证后端**
   ```bash
   # 健康检查
   curl http://localhost:8000/health
   # 学校列表
   curl http://localhost:8000/v1/config/schools
   ```

3. **启动 Flutter**
   ```bash
   cd classduck_app
   flutter run -d chrome    # 或其他目标
   ```

4. **执行导入**
   - 课程表页 → 点击加号 → 教务添加
   - 选择学校（如 XJTU）
   - 进入导入执行页 → 打开登录页 → 登录
   - 点击下载按钮执行导入

5. **检查日志**
   ```bash
   curl http://localhost:8000/api/import/log   # 查看是否有日志写入
   ```

## 5. 注意事项

1. **后端先于前端启动**：Flutter 启动时不会主动连后端，但导入、学校列表等功能依赖后端在线。学校列表有本地 builtin 兜底。
2. **WebView 仅原生端可用**：Web 端无法使用内嵌 WebView，会提示不可用。
3. **数据库文件位置**：
   - Python 日志 DB：`backend/python_import_service/data/db/logs.db`（自动创建）
   - Flutter 课表 DB：由 sqflite 管理，在设备应用沙箱内
4. **端口冲突**：若 8000 被占用，改用其他端口并同步修改 `AppEnv.apiBaseUrl`。
5. **CORS**：开发阶段允许全部来源；生产部署必须收紧 `allow_origins`。
