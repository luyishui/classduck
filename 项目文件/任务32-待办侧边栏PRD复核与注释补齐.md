# 任务32：待办侧边栏 PRD 复核与注释补齐

## 1. 本次目标
1. 按 `待办侧边栏.md` 逐条复核当前实现。
2. 补足代码中的状态机注释，明确普通态/激活态迁移。
3. 同步维护项目文件与说明文档，避免“只改代码不记账”。

## 2. PRD 对照结论
- 已对齐：
  1. 顶部品牌区（鸭 + 上课鸭）。
  2. 普通态展示分组 + `+ 新建待办`。
  3. 点击 `+ 新建待办` 进入激活态，出现输入行 + 勾叉。
  4. 确认后写入类型并联动主页面分组/新建待办类型集合。
  5. 取消或关闭侧栏时不落库。
- 已增强：
  1. 长按自定义类型可进入删除待确认态（左侧删除插件）。

## 3. 代码层变更
- 文件：`classduck_app/lib/features/todo/ui/todo_page.dart`
- 变更：
  1. 为 `_openSidebarPlaceholder()` 增加 PRD 状态机注释。
  2. 为确认创建、长按删除、激活态切换补注释。

## 4. 说明
- 本次重点为“复核 + 注释 +文档同步”；不引入额外结构性改动。
- 后续若继续做像素级细节，将按你下一轮反馈继续对齐。
