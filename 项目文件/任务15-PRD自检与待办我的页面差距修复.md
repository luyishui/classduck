# 任务15-PRD自检与待办我的页面差距修复

## 1. 任务目标
根据产品经理要求，改为“研发自检 + 对照 PRD 验收标准”模式，先输出差距再修复关键偏差，重点覆盖待办页与我的页。

## 2. 自检范围
- `参考文件/操作逻辑说明和PRD/待办主页面-未完成-抽屉拉开.md`
- `参考文件/操作逻辑说明和PRD/待办主页面-已完成.md`
- `参考文件/操作逻辑说明和PRD/我的主页面.md`
- `参考文件/操作逻辑说明和PRD/提醒与通知.md`

## 3. 发现的关键差距（修复前）
1. 待办页结构偏差
- 缺少“未完成/已完成”分栏切换。
- 缺少按任务类型分组的抽屉展开/收起。
- 缺少侧边栏按钮与分享按钮。

2. 我的页结构偏差
- 顶部未体现分享按钮。

## 4. 本次修复内容
### 4.1 待办页重构
文件：`classduck_app/lib/features/todo/ui/todo_page.dart`

修复点：
1. 增加 `TabBar + TabBarView` 实现“Pending/Completed”分栏，并支持左右滑动切换。
2. 增加任务类型分组（assignment/exam/contest）与抽屉展开收起状态。
3. 增加“默认最多展示3条，展开显示全部”逻辑。
4. 增加左上菜单与右上分享按钮占位交互。
5. 保留新建待办入口与勾选完成迁移逻辑。

### 4.2 我的页补齐分享按钮
文件：`classduck_app/lib/features/profile/ui/profile_page.dart`

修复点：
1. 增加右上分享按钮占位交互，满足 PRD 顶栏要求。

## 5. 核心函数说明
### 5.1 `TodoPage._buildTodoTab(...)`
- 输入：任务列表、完成态
- 输出：按类型分组的分栏内容
- 逻辑：构建类型标题、抽屉状态、条目列表。

### 5.2 `TodoPage._buildGroupItems(...)`
- 输入：类型、数据、展开态
- 输出：该组可见任务 Widget 列表
- 逻辑：收起态最多3条，展开态显示全部。

### 5.3 `TodoPage._openSidebarPlaceholder()` / `_openSharePlaceholder()`
- 输入：无
- 输出：占位弹窗
- 逻辑：保留交互位置，后续接真实功能。

## 6. 验收标准（本次）
1. 待办页支持分栏点击切换与滑动切换。
2. 待办页支持类型分组抽屉和预览条数策略。
3. 待办页顶部具备侧边栏和分享按钮。
4. 我的页具备分享按钮。
5. `flutter analyze` 通过。

## 7. 验收结果
- 所有修复项完成，静态检查通过：`No issues found`。

## 8. 剩余差距（下轮计划）
1. 提醒页开关视觉尚非 PRD“胶囊推拉”终态。
2. 每日提醒时间弹窗尚未实现滚轮视觉。
3. 待办侧边栏“新增任务类型并联动分组”仍为占位能力。
