# 任务27-侧边栏完整界面落地（课表+待办）

## 1. 任务目标
将课表页与待办页左上角菜单从占位弹窗升级为“完整侧边栏界面”，并与 PRD/Pen 的左侧抽屉交互保持一致。

## 2. 本次改动文件
1. classduck_app/lib/features/schedule/ui/schedule_page.dart
- 新增课表侧边栏弹出逻辑（左侧滑入、遮罩关闭）。
- 增加课表项卡片列表，支持切换当前课表并刷新课程数据。
- 新增 `_activeTableId` 追踪当前激活课表。
- 新增 `_SidebarCourseCard` 复用组件。

2. classduck_app/lib/features/todo/ui/todo_page.dart
- 将任务类型管理从小弹窗升级为完整左侧边栏面板。
- 面板内提供：类型卡片、`+ 新建待办`、新增类型输入与确认。
- 新增 `_TodoSidebarCard` 复用组件。

## 3. 关键函数说明
### 3.1 Schedule
1. `_openScheduleSidebar()`
- 使用 `showGeneralDialog` 创建左侧抽屉。
- 列出课表卡片，并以高亮展示当前激活项。

2. `_switchCourseTable(tableId, tableName)`
- 切换课表后刷新 `_activeTableId/_activeTableName/_courses`。
- 保证从侧边栏切换后主页面立即生效。

### 3.2 Todo
1. `_openSidebarPlaceholder()`（已升级为完整侧边栏）
- 负责展示任务类型卡片和新增流程。
- 新增类型时同步更新 `_typeOrder/_typeLabels` 与展开状态字典。

## 4. 与 PRD/Pen 对齐点
1. 侧边栏为左侧抽屉，不再是中间小弹窗占位。
2. 抽屉面板具备：遮罩关闭、滑入动效、卡片化内容组织。
3. 待办侧边栏保留“+ 新建待办”作为高亮动作入口。

## 5. 验证结果
1. flutter analyze：No issues found。
2. 核心交互验证：
- 课表页可打开侧边栏并切换课表。
- 待办页可打开侧边栏并新增类型。

## 6. 后续衔接
下一步按用户顺序继续：
1. 选择学校真实分栏过滤（专科/本科/硕士/通用）。
2. 补齐剩余二级页面并持续像素级对齐。