# 任务28-选择学校真实分栏过滤与契约同步

## 1. 目标
将“选择学校”顶部四分栏从纯视觉切换升级为真实数据过滤，并同步前后端契约字段，确保专科/本科/硕士/通用分栏与列表数据一致。

## 2. 本次改动
1. 前端域模型
- 文件：classduck_app/lib/features/import/domain/school_config.dart
- 新增字段：`level`
- 新增兼容逻辑：当后端旧数据无 `level` 时，按 id/title 自动推断层级。

2. 前端筛选逻辑
- 文件：classduck_app/lib/features/import/ui/import_school_list_page.dart
- `_filteredConfigs()` 改为：
  1) 先按当前分栏 level 筛选
  2) 再按关键字筛选
- 新增 `_schoolLevelKeys` 与分栏索引映射。

3. 后端示例配置
- 文件：backend/src/config/schools.sample.json
- 为学校配置新增 `level` 字段。
- 增加多层级示例（junior/undergraduate/master/general）。

4. 契约同步
- 文件：contracts/schemas/school-config-list.schema.json
- 新增 `level` 枚举字段并设为 required。
- 文件：contracts/changelog/CHANGELOG.md
- 记录本次契约扩展。

5. 说明文档同步
- 文件：说明文档/02-核心模块函数与交互逻辑.md
- 文件：说明文档/03-设计修改记录.md

## 3. 核心函数说明
### 3.1 SchoolConfig.fromMap
- 先读取后端 `level`。
- 若无 `level`，使用 `_normalizeLevel()` 进行兼容推断。

### 3.2 ImportSchoolListPage._filteredConfigs
- 当前分栏对应 level：
  - 专科 -> junior
  - 本科 -> undergraduate
  - 硕士 -> master
  - 通用 -> general
- 过滤顺序：层级过滤优先，关键词过滤其次。

## 4. 验证结果
- flutter analyze：No issues found。

## 5. 后续
- 继续推进剩余 UI 与后端内容，下一步进入“适配规则接入前”的剩余缺口清理。