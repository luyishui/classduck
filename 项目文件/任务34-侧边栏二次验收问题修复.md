# 任务34：侧边栏二次验收问题修复

## 1. 用户反馈对应修复
1. 课程表侧栏动画升级为复合动效（淡入+滑入+轻缩放）。
2. 课表设置中新建课表输入行与课表列表文本对齐。
3. 上午/下午/晚上改为“课程节数”语义，按承接关系自动计算节次区间。
4. 各分组加减按钮颜色改为分组主题深色，不再统一蓝色。
5. 周数设置滚轮支持循环滚动，并调整选中遮罩避免遮挡文字。
6. 开学日期滚轮去除“年/月/日”后缀，只保留阿拉伯数字。
7. 步进控件中间白色值支持点击后手动输入数字。
8. 待办侧边栏垃圾箱移到右侧，类型文本左对齐。
9. 新建待办类型支持“最多3个 + ... 更多弹层”。
10. 新建待办课程选项改为从课表真实课程动态加载。
11. 我的页面已完成待办分组去掉“其他”，未知类型直接显示原类型名。

## 2. 核心文件
- `classduck_app/lib/features/schedule/ui/schedule_page.dart`
- `classduck_app/lib/features/todo/ui/todo_page.dart`
- `classduck_app/lib/features/todo/ui/new_todo_page.dart`
- `classduck_app/lib/features/profile/ui/profile_page.dart`

## 3. 验证
- 静态检查：`flutter analyze` 通过。
- 运行验证：`flutter run -d chrome --web-port 4088` 启动成功。
